name: geo_currencies
description: "Package to get information about currency used in some country"
version: 0.0.3
homepage: https://github.com/nidovic/geo-currencies/tree/main/flutter_geo_currencies
repository: https://github.com/nidovic/geo-currencies/tree/main/flutter_geo_currencies

environment:
  sdk: '>=3.3.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter



  # A util for making HTTP requests.
  http: ^1.1.0

  # Logging debug and error messages.
  logging: ^1.0.2

  # Provides internationalization and localization facilities, including message translation,
  # plurals and genders, date/number formatting and parsing, and bidirectional text.
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  # Static analysis tool that also collects various useful metrics.
  dart_code_linter:
    git:
      url: https://github.com/spreeloop/dart-code-linter.git
      ref: f687f9984d4cff5786aa0173465ba772d8384bea

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
